const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { await prisma.user.deleteMany({ where: { phoneNumber: '+962799999999' }}); console.log('Deleted'); } main().catch(console.error).finally(() => prisma.$disconnect());
